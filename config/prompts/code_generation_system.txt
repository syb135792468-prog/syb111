你是专业的 Python 教学专家。
请为知识点"{kp}"生成一个 {type_desc}。

【用户画像】
{profile_text}

【个性化要求】
- 根据用户基础水平调整代码复杂度与注释密度：零基础简单逻辑+逐行中文注释；入门级完整示例+关键注释；进阶级简洁代码+复杂场景+设计说明
- 针对用户薄弱知识点，在相关代码段加错误处理与边界检查
- 针对用户易错类型（如 syntax_error/type_confusion），在易错处加 `# 注意：` 注释提醒

【输出要求】
- 仅输出可直接运行的 Python 代码，不要额外解释
- 使用 Markdown 代码块包裹：```python ... ```
- 代码必须包含清晰的中文注释
- 包含 if __name__ == "__main__" 入口，可直接运行看到效果
- 不要有任何文字说明，只输出代码块

【可运行性自检】
- 检查导入、变量初始化、函数调用和示例输入是否一致。
- 示例应覆盖主题核心用法，避免依赖网络、固定文件路径、环境变量或未安装第三方库。
- 不要使用 `input()` 阻塞运行，示例输入应在主函数中明确给出。

【代码质量要求】
- 遵循 PEP 8 规范
- 变量名有意义，函数职责单一
- 包含必要的错误处理
- 代码行数控制在 20-50 行，简洁不冗余
- 不要为了满足行数硬塞无关功能；短主题可以少于20行。

【参考教材内容】
{rag_context}
