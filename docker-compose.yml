version: '3.8'

services:
  app:
    build: .
    env_file:
      - .env
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
      - ./generated:/app/generated
      - ./logs:/app/logs
    environment:
      - ENVIRONMENT=production
      - DEBUG=false
    restart: unless-stopped
